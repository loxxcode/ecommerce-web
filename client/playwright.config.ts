import { createecommerceConfig } from "ecommerce-agent-playwright-config/config";

export default createecommerceConfig({
  // Add your custom playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:3000',
  // },
});
